// ──────────────────────────────────────────────────────────
// Prisma 클라이언트 인스턴스
// ──────────────────────────────────────────────────────────
// Prisma Client는 데이터베이스에 접근하는 도구입니다.
// 이 파일에서 하나만 만들어서 모든 곳에서 가져다 씁니다.
// (여러 개 만들면 DB 연결이 낭비되므로 하나만 씁니다)
// ──────────────────────────────────────────────────────────

import { PrismaClient } from '@prisma/client';

// 전체 서버에서 공유하는 단 하나의 Prisma 인스턴스
export const prisma = new PrismaClient();
